как построить lineage? и нужен ли он? или нужен, но не lineage? а что тогда? и каким инструментом его лучше визуализировать? у меня есть большой etl-проект и хранилище данных. много источников: различные базы данных, веб-сервисы, и т.п. создано решение на c#, которое называется mETL - оно загружает данные из источников в слой staging хранилища и работает как ELT, однако часть данных загружается SSIS-пакетами, в которых может происходить трансформация данных "на лету" - ETL. Ядро хранилища - это спагетти, и что-то складывается сразу в него, а что-то проходит через Staging. Основным способом трансформации данных являются SSIS-пакеты. Однако, так же много логики реализовано в виде хранимых процедур, которые запускаются из SSIS-пакетов и SQL Agent Jobs, а многое - в коде views. С помощью этого запутанного механизма данные попадают в витрины, на которых строятся отчёты и кубы Analysis Services. В этих кубах в слое DSV так же много именованных запросов, в которых зашита какая-то бизнес-логика. Интересно было бы видеть, как данные на уровне отдельных полей трансформируются от источника до получателя. Чтобы отвечать на три категории вопросов: со стороны отчётов и кубов - из чего и откуда взялось это значение? со стороны источников: на что повлияет изменение того или иного поля? из центра: каких потребителей затронет изменение какого-то срединного кирпичика всего etl, на что оно может повлиять и от каких входных данных зависит? в lineage хочется буквально видеть каждый шаг, откуда и куда переезжают данные, что с ними происходит и какими механизмами это осуществляется, а так же адрес - где лежит код, который это делает - в пакете SSIS, DSV куба, хранимой процедуре, представлении или это mETL, включая таблицы хранилища, через которые эти данные текут. Сейчас всё зависит от всего, а хочется построить по возможности атомарные независимые друг от друга потоки для большей управляемости, чистоты и упрощения понимания того, как всё работает. поисследуй и предложи, что со всем этим хозяйством можно сделать.


я пытался решить эту задачу и вот что было сделано: извлечены атрибуты кубов - меры и измерения, а так же named query из которых они строятся, а так же код всех представлений, хранимых процедур и функций используемых баз данных, а так же набор всех ssis-пакетов и sql agent jobs. что касается metl, то он перекладывает данные из источника в staging 1:1 не меняя ни имён таблиц, ни полей и есть простая таблица настроек, которую можно врезать в эти данные как часть lineage. То есть, мне нужно просто из всего этого теперь построить lineage и записать его в виде графа в некий набор таблиц, и ещё нужен инструмент, которым потом это можно визуализировать. как бы ты поставил задачу Claude Code на разработку такого решения на c#, которое на регулярной основе будет разбирать этот объём данных, парсить sql-запросы (диалект ms sql server)  и обновлять этот граф в виде нескольких таблиц? что бы ты предложил для визуализации такого графа?